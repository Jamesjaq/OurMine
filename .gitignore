# Local engagement/runtime artifacts (proof packs, wallets, Tor keys, sessions)
.ourmine/

# Secrets / local runtime config (never commit)
.env
.env.local
**/.env.local
.cursor/mcp.json

# Test temp dirs
packages/security/test/.tmp_*/

# ARES Operational Purity: Ignore Labs and Local Binaries
/lab/
/packages/security/src/ares/bin/
